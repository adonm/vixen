name: vixen_shell
description: Vixen's production Flutter shell and isolated native browser bridge.
publish_to: 'none'
version: 0.1.6+6

environment:
  sdk: ^3.13.0-0
  flutter: '>=3.46.0-0.3.pre'

dependencies:
  ffi: ^2.2.0
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
