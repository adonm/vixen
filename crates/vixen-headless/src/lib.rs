//! vixen-headless — headless CLI + CDP server.
//!
//! Stub at Phase 0 (docs/PLAN.md). Phase 8 implements the full CLI flag
//! surface (docs/SPEC.md "Headless CLI surface") and the CDP server, using
//! `SurfacelessSurface` (EGL surfaceless) as the `GlContext` impl (ADR-009).

#![forbid(unsafe_code)]

pub mod placeholder;
