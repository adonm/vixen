//! vixen-core — engine integration glue.
//!
//! Stub at Phase 0 (docs/PLAN.md). Phase 2 stands up the SpiderMonkey
//! runtime, Phase 3 the HTML parse + Stylo cascade, Phase 4 layout,
//! Phase 5 the single WebRender paint path consuming `vixen_api::GlContext`.

#![forbid(unsafe_code)]

pub mod placeholder;
